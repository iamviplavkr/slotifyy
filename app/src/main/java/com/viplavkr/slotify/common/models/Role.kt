package com.viplavkr.slotify.common.models

/**
 * Defines the two roles in the Slotify ecosystem.
 * Used by AuthManager to route users to the correct dashboard.
 */
enum class Role {
    USER,
    ADMIN
}
